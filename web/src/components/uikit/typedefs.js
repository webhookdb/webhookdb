/**
 * @typedef {('sm'|'md'|'lg')} Size
 */

/**
 * @typedef {(1|2|3|4|5|6|7)} Spacing
 */

/**
 * @typedef {('blurple'|'secondary'|'grey'|'orange'|'red'|'green'|'blue')} ThemeColor
 */

/**
 * @typedef {('success'|'warning'|'error'|'info')} ActionVariant
 */

/**
 * @typedef {import('@fortawesome/pro-solid-svg-icons').IconDefinition} Icon
 */

/**
 * @typedef TimeRange
 * @property {string} lower
 * @property {string} upper
 * @property {number} elapsed
 * @property {boolean} past
 * @property {boolean} future
 * @property {boolean} current
 */
